# Quick Start

```bash 
$ git clone https://github.com/sokratisvas/jack-compiler
$ cd jack-compiler
```
Currently only the Tokenizer is implemented:
```bash 
$ cargo run
$ cd tests
$ python3 filecomp.py
```
